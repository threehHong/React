1. 리액트에서 class는 자바스크립트 예약어 이므로 className으로 클래스명을 입력해줘야 한다.


2. 컴포넌트 return에서 태그 하나만 사용할 수 있다 따라서 여러개의 태그를 사용할 경우 div 태그로 묶어주거나 <> </>로 묶어준다.
   Fragment라고 하고 브라우저 즉 개발자 도구에서는 표시되지 않는다..


3. 함수형 컴포넌트는 함수를 의미
렌더링은 함수형 컴포넌트를 호출하는 것.
렌더링 될 때 마다 컴포넌트 내부의 변수는 초기화 된다.


4. index.js
<React.StrictMode>가 있으면 App 컴포넌트에 있는 콘솔 두 번 출력됨
StrictMode 태그를 사용해서 개발하는 것이 좋다 여러가지 문제점을 알려주기 때문
